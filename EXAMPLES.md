# 使用示例

## 基本使用

### 1. 转换 PDF 文件

1. 选择 "PDF 文件" 标签
2. 选择 AI 提供商和模型
3. 选择输出格式 (Markdown / HTML / 纯文本)
4. 拖拽 PDF 文件到上传区域
5. (可选) 开启末尾追加内容
6. 等待处理完成，查看预览并下载

### 2. 转换图片文件

1. 选择 "图片文件" 标签
2. 上传图片 (PNG、JPG 等)
3. 选择输出格式
4. 等待处理完成

### 3. 使用图片 URL

1. 选择 "图片 URL" 标签
2. 粘贴在线图片 URL
3. 点击处理

## 输出格式说明

### Markdown (默认)

- 标准 Markdown 语法
- 表格使用 HTML `<table>` 格式 (支持合并单元格)
- 代码块、公式、链接等完整保留

### HTML

- 纯 HTML 结构
- 使用 `<h1>`-`<h6>`、`<p>`、`<ul>`、`<table>` 等标签
- 适合直接嵌入网页

### 纯文本

- 无格式标记
- 表格使用 `|` 分隔
- 适合复制粘贴

## 提示词示例

可在"自定义提示词"框中输入，覆盖默认提示词：

### 表格识别优化

```
请识别图片中的表格数据，确保所有单元格内容完整，使用 HTML 表格格式输出。
```

### 代码识别

```
请识别图片中的代码，并使用正确的代码块格式输出，标注编程语言。
```

### 数学公式

```
请识别图片中的数学公式，使用 LaTeX 格式输出。
```

### 多语言文档

```
请识别图片中的中英混排内容，保持原有格式。
```

## 高级配置

### 调整并发数

编辑 `server/.env`：

```env
OCR_CONCURRENCY=5   # 同时处理 5 页
```

### 调整文件大小限制

编辑 `server/src/index.js` 中的 multer 配置：

```javascript
limits: { fileSize: 100 * 1024 * 1024 } // 100MB
```

### 自定义末尾追加内容

编辑 `/prompts/append-content.md`，或在前端文本框中修改。

## 注意事项

1. API 密钥需有足够配额
2. 大型 PDF (50+ 页) 处理时间较长
3. 复杂表格可能需要调整提示词
4. 扫描件质量影响识别准确度
