# 使用示例

## 基本使用

### 1. 转换PDF文件

1. 点击"PDF文件"标签
2. 拖拽PDF文件到上传区域
3. （可选）在提示词框中输入：`请识别文档内容，保持原有的标题层级结构`
4. 点击上传后等待处理
5. 查看预览并下载Markdown文件

### 2. 转换图片文件

1. 点击"图片文件"标签
2. 上传图片（支持PNG、JPG、JPEG等格式）
3. （可选）使用自定义提示词：`请识别图片中的表格内容，并用Markdown表格格式输出`
4. 等待处理完成
5. 下载转换结果

## 提示词示例

### 中文文档识别
```
请识别图片中的中文内容，保持原有的格式和结构，以Markdown格式输出。
```

### 英文文档识别
```
Please recognize the English text in the image and output in Markdown format.
```

### 表格识别
```
请识别图片中的表格数据，并用Markdown表格格式输出。
```

### 代码识别
```
请识别图片中的代码，并使用正确的代码块格式输出。
```

### 数学公式
```
请识别图片中的数学公式，并使用LaTeX格式输出。
```

### 保持格式
```
请识别图片内容，保持原有的标题、列表、粗体等Markdown格式。
```

## 多页PDF处理

系统会自动处理多页PDF文件：
- 如果PDF包含可提取的文本，直接转换为Markdown
- 如果是扫描版PDF，将每页转换为图片后进行OCR识别
- 所有页面内容会合并为一个Markdown文件，按页分隔

## 输出格式说明

转换后的Markdown文件包含：
- 页面标题（对于多页PDF）
- 识别的文本内容
- 保持原有的标题层级
- 列表和段落格式
- （可选）表格格式（根据提示词）

## 高级功能

### 自定义API端点
如果需要使用不同的API端点，修改 `client/src/services/api.js` 中的 `API_BASE_URL`。

### 调整文件大小限制
修改 `server/src/index.js` 中的 multer 配置：
```javascript
limits: { fileSize: 50 * 1024 * 1024 } // 当前是50MB
```

### 自定义样式
修改 `client/src/index.js` 中的 Material-UI 主题配置。

## 注意事项

1. 确保DeepSeek API密钥有效且有足够的配额
2. 大文件处理可能需要较长时间，请耐心等待
3. OCR识别的准确度取决于图片质量
4. 建议使用高清晰度的图片或PDF以获得更好的识别效果
5. 复杂的表格和公式可能需要多次调整提示词才能获得理想结果
